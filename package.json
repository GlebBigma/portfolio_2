{
  "name": "portfolio_2",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "sass": "^1.50.0",
    "vite": "^2.9.0"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  }
}
